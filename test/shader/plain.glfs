uniform float time;
uniform vec4 colourMod;
//uniform sampler2D pic;
varying vec2 texCoord;

void main() {
    gl_FragColor = colourMod + vec4(sin(time) * 0.5 + 0.5, sin(gl_FragCoord.y / 2.0 - time * 10.0), 0.3, 0.7);
}
