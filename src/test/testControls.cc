#include "catch.hh"
#include "../Utils.hh"
#include "helpers.hh"
#include <SFML/Graphics.hpp>

TEST_CASE("smoke is not smelled coming from controls", "[controls][smoke]") {
    // TODO: create a little fake scene class that just messes around with controls a bit and make sure that it works.
}
