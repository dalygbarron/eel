#ifndef CAMEL_H
#define CAMEL_H

/**
 * Namespace for generalised graphical user interface code. Nothing in Camel namespace should include code from wider
 * project at all. That way it can be used for other stuff as well if need be.
 */
namespace Camel {

};

#endif
