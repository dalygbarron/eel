#ifndef ACTOR_H
#define ACTOR_H

/**
 * Represents a general dude who can participate in a bullet hell scene and maybe other scenes too.
 * For the time being actors are gonna use a texture each, but if I ever need to render a couple of hundred at once
 * then maybe I can rethink that.
 */
class Actor {

};

#endif
